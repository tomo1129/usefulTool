<%= render 'shared/header' %>
<div class="main">
	<article>
		<section class="recent_posts">
			<h2>最新投稿</h2>
			<% if @recentPosts %>
				<ul>
				<% @recentPosts.each do |post| %>
					<li><a href="/knowledge/<%= post.id %>"><%= post.title %></a></li>
				<% end %>
				</ul>
			<% end %>
		</section>
		<!--最新ニュース(About Latest News)とか-->
		<!--特集とか-->
		<!--便利ツールとか-->
		<!--閲覧ランキング-->
	</article>
	<%= render 'shared/sidebar' %>
</div>
<%= render 'shared/footer' %>